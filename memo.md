## std::pair ##
2 つの値をひとまとめにして保持するクラス。
std::map は同じキーを複数持ていないため、insert を実施したあとに以下の情報を返す
first: 挿入された要素、またはすでに存在していた要素のイテレータ
second: 新しく挿入できた場合は true キーがすでに存在した場合は false
コンテナではない

## std::string::npos ##
string::find で値が見つからなかった場合に返す値として定義されている。

## std::lower_bound ##
x  を右辺とする  <  演算が成り立たない最初の要素を指すイテレータを返す
-> x に与えた値以上の数値を返す。
```cpp
#include <iostream>
#include <map>
#include <string>

int main() {
    // 1. Map という名のプレイリストを作成
    std::map<int, std::string> playlist = {
        {10, "Classic Soul"},
        {20, "Funky Disco"},
        {30, "Electro House"},
        {40, "Chill Beats"}
    };

    int target_key = 25; // 25番の曲を探したいが、リストにない
    // 2. lower_bound 発動！ 25「以上」の最初の要素を探す
    auto it = playlist.lower_bound(target_key);
    std::cout << "--- Searching for Key 25 ---" << std::endl;
    // 3. 安全チェック（end() かどうかを確認）
    if (it != playlist.end()) {
        // ここがポイント：25はないので、25より大きい「30」がヒットする
        std::cout << "Found element: [" << it->first << "] -> " << it->second << std::endl;
    } else {
        std::cout << "Nothing found... the party is over!" << std::endl;
    }
    return 0;
}
```

## 逆ポーランド記法 (Reverse Polish Notation) ##
演算子を数字の後ろに書く計算式の表現方法
通常の式では
```
3 + 4
```
と記載するが、逆ポーランド記法では
```
3 4 +
```
と記載する。
その他の例
例 1 
```
(3 + 4) × 2
```
逆ポーランド記法
```
3 4 + 2 *
```
例 2
```
3 + (4 × 2)
```
逆ポーランド記法
```
3 4 2 * + 
```
基本ルールとして下記が成り立つ
1. 数字が来たらスタックに入れる
2. 演算子が来たら数字を2つ取り出す
3. 計算結果をスタックに戻す
4. 最後に残った値が答え

## Merge-Insertion Sort ##
### 何がしたいのか ###
別名 Ford-Johnson algorithm のこと。
できるだけ比較回数を少なくするという観点で使用されるアルゴリズム。
アイデアとしては、小さいものを全部そのまま挿入するのではなく、各要素についてここより後ろにはいかないという上限を利用し、二分探索が最も効率よく働く順番で挿入する。

このソートでは、実行時間ではなく、大小比較を何回行う必要があるかを中心に考えている。
例として、下記の 3 つの数値を並べるとする。
```
8 3 5
```
比較によってわかることは、基本的に次の 2 つである
```
8 < 3
8 > 3
```
このため、1 回比較を行うと得られる答えは上記 2 種類であり、可能性を最大 2 つに分割できる。
比較を繰り返すたびに、可能性は 2 の乗数倍増えていく。
例えば 3 回比較を繰り返す場合、
```
2 * 2 * 2 = 8
```
8 通り区別することが出来る。

一方 3 個の要素の異なる値の並び順は
```
3! = 3 *2 * 1 = 6 通り
```
2 回の比較では、最大 4 通りしか区別できないため、6 通りを完全に見分けられない。
したがって、3 この値のソートには少なくとも 3 回の比較が必要。
この考えを一般化すると下記の数式になる。
$$
⌈log2​(n!)⌉
$$
### 全体像 ###
Merge-Insertion sort は下記の流れで進む
1. 要素を2個ずつのペアにする
2. 各ペアを 1 回比較し、小さいほうと大きいほうに分ける
3. 各ペアの大きいほうだけを再帰的にソートする
4. ソート済みの大きいほうを中心に最初の整列済み列を作る。
5. 残っている小さい方を特殊な順番で二分挿入する。

大きいほうを先にソートする部分がマージ的であり、小さいほうを後から挿入する部分が挿入ソート的なので、Merge-insertion と呼ばれる。
:::note
気になる点 要素数が奇数の場合どうなるのか

### 例 ###
下記 8 個の要素があるとする
```
7 2 9 1 5 3 8 4
```
2 個ずつをペアにする
```
(7,2)
(9,1)
(5,3)
(8,4)
```
それぞれ一回ずつ比較し、小さいほうと大きいほうを決める
```
(小:2, 大:7)
(小:1, 大:9)
(小:3, 大:5)
(小:4, 大:8)
```
Merge-insertion sort では通常、大きいほうを a 小さいほうを b とあらわすそのため、下記のようになる。
```
(b=2, a=7)
(b=1, a=9)
(b=3, a=5)
(b=4, a=8)
```
各ペアについて下記であることが、保証される。
$$
bi​≤ai​
$$

### ソート開始 ###
大きいほうだけを取り出し、再帰的に Merge-Insertion Sort で並び替える。
Before
```
7  9  5  8
```
After
```
5  7  8  9
```
なお、a を並び替えるときには対応する b も一緒に移動させる。
ソート後のペアは下記。
| 番号 | 小さい方 `bᵢ` | 大きい方 `aᵢ` |
| -: | --------: | --------: |
|  1 |         3 |         5 |
|  2 |         2 |         7 |
|  3 |         4 |         8 |
|  4 |         1 |         9 |

下記の関係が成り立つことが分かる。
$$
a1 ≤ a2 ≤ a3 ≤ a4
$$
さらに各ペアでは下記がすでに判明している。
```
b1 ≤ a1
b2 ≤ a2
b3 ≤ a3
b4 ≤ a4
```

ただし、 b 同士の順番はわからない。

### Main chain の作成 ###
ソートされた a は下記である。
```
a1  a2  a3  a4
```
ここから、b1 のみは、a1 の前に置くことができる。
これは、下記を満たすためである
```
b1 ≤ a1 && a1 ≤ a2 ≤ a3 ≤ a4
```
このため、下記が保証される。
```
b1 ≤ a1 ≤ a2 ≤ a3 ≤ a4
```
これを main chain と呼ぶ。
残る b2, b3, b4 は後から main chain へ挿入する。残っている要素のことを pending elements 等と呼ぶ。

### bi の挿入位置の走査対象 ###
例えば b3 を挿入する場合、 b3 と a3 はもともと同じペアであったため、下記が決まっている。
```
b3 ≤ a3
```
このため、b3 が a3 より後ろに入ることはない。
そのため、 b3 の挿入位置は、main chain の中の下記で十分
```
b1 a1 a2
```

Merge-Insertion sort では、対応する ai より前だけを二分探索する。これにより比較回数を減らせる。

### 二分探索 ###
二分探索では、要素数そのものよりも、挿入可能な場所の数を考えるとわかりやすい。
例えば、3 つの要素では、新しい値が入る可能性がある箇所は 4 箇所になる。
```
_ A _ B _ C _
```
そのため、4 通りである場合は、 2 回の比較で判別することが出来る。
3 要素への挿入は最大 2 回の比較で可能である。
同様に下記のようになる。
| 検索対象の要素数 | 挿入位置の数 | 必要な比較回数 |
| -------: | -----: | ------: |
|        1 |      2 |       1 |
|        3 |      4 |       2 |
|        7 |      8 |       3 |
|       15 |     16 |       4 |
|       31 |     32 |       5 |

これは
$$
2^k−1
$$
この要素に対して、二分挿入すると、挿入候補がちょうど
$$
2^k
$$
になることを表す。そのため、どの位置に入る場合でも最大 k 回の比較で判定できる。
Merge-Insertion sort は検索範囲ができるだけ
```
3, 7, 15, 31, ...
```
になるように挿入順序を調整している。
ここが、単純な二分挿入よりに書く回数を減らせる理由。
### 挿入の順番 ###
Merge-Insertion sort では下記の様な順番を使用する。
```
b3, b2,
b5, b4,
b11, b10, b9, b8, b7, b6,
b21, b20, ..., b12,
...
```
この順番は、検索対象のサイズを
```
3
7
15
31
...
```
付近に保つために使用される。
:::note
何故、この順番なのか？

### Jacobsthal 数列 ###
挿入順序の区切りには
```
1,  3,  5,  11,  21,...
```
という数列が使用される。
これを Jacobsthal 数列と呼ぶ。
次の関係を満たす数列として導出されている。
$$
t_k-_1 + t_k = 2^k
$$
具体的には下記となる。
```
1 + 3  = 4  = 2²
3 + 5  = 8  = 2³
5 + 11 = 16 = 2⁴
11 + 21 = 32 = 2⁵
```
この区切りを使用し、各範囲を逆向きに処理する。
```
2～3   → 3, 2
4～5   → 5, 4
6～11  → 11, 10, 9, 8, 7, 6
12～21 → 21, 20, ..., 12
```
これが実際の挿入順序となる。

### 何故各グループを逆順に挿入するのか ###
例として
```
b6, b7, b8, b9, b10, b11
```
というグループがあるとする。
これを
```
b11, b10, b9, b8, b7, b6
```
の順で挿入する。
b11 は対応する a11 より前を検索する。次に b10 は a10 より前だけを検索する。
b11 を先に挿入したことで、main chain 全体は 1 要素増えている。しかし、次は検索上限が、
```
a11 → a10
```
と一つ前に移る。
つまり、挿入で一つ要素が増える一方で、検索上限が 1 つて前になるという 2 つがほぼ打ち消しあう。
そのため、グループ内の各要素を挿入するときの検索範囲が、同じ比較回数で処理できる大きさに保たれる。

### 奇数個の要素の例 ###
21 この要素の場合、まず 10 組のペアと 1 この余りを作る。
各ペアの大きいほうを
```
a1, a2, ..., a10
```
小さいほうを
```
b1, b2, ..., b10
```
余った要素を
```
b11
```
とする。
大きいほうを再帰的にソートし、そこから、main chain を作成する。
```
b1, a1, a2, ..., a10
```
残りの挿入順序は下記
```
b3, b2,
b5, b4,
b11, b10, b9, b8, b7, b6
```

b11 はペアを持たないあまりの要素なので、対応する a11 がない。そのため、 main chain 全体を検索する必要がある。
この時点で main chain の要素数がちょうど 15 になるように調整されている。
15 要素であれば、挿入位置は 16 通りなので、 4 回の比較で挿入できる。
その後の 
```
b10, b9, b8, b7, b6
```
も 4 回の比較で挿入できる。
総比較回数は、
$$
10+S(10)+2+2+3+3+4+4+4+4+4+4
$$
である。
Binary Insertion では 74 回必要なところを Merge-Insertion では 66 回に抑えている。

:::note
S(10) とは何か

### 何故 21 要素では 66 回が完全に最適なのか ###
21 個の異なる要素には
$$
21!
$$
通りの並びがある。
$$
26^5<21!<26^6
$$
であり、65 回の比較では、最大でも
$$
2^65
$$
通りしか区別できない。
しかし、実際の並び方はそれより多いため、65 回では絶対に足らない。
したがって、どんなソートアルゴリズムでも最悪の場合少なくとも 66 回は比較が必要。
Merge-Insertion Sort は 66 回でソートできた。
したがって、これ以上比較回数を減らせないことが証明される。
これを最悪比較回数の意味で最適という。

### 一般的なアルゴリズムの流れ ###

要素数を n とした場合、下記のように整理される。
1. ペアを比較する。
$$
⌊n/2⌋
$$
組のペアを作成する。⌊ ⌋ は小数点以下を切り捨てる記号。
例えば
```
n = 8 → 4組
n = 9 → 4組 + 余り1個
```
となる。

2. 大きいほうを再帰的にソートする。
各ペアの大きいほう ai だけをMerge-Insertion Sort でソートする。
ただし、対応するbi との関係を保持したまま、ペア全体を並べ替える。

3. Main chain を作る
```
b1, a1, a2, ..., am
```
を main chain とする。
ここで、
$$
m=⌊n/2⌋
$$
となる。

4. 残りの b を特殊な順番で挿入する。
```
b3, b2,
b5, b4,
b11, b10, ..., b6,
b21, b20, ..., b12,
...
```
の順で挿入する。
各 bᵢ は対応する aᵢ より小さいため、aᵢ より前だけを二分探索する。
余りの要素には対応する a がないため、main chain全体を検索する。

### その他の注意点 ###
Merge-Insertion Sort は常に絶対最適なアルゴリズムではないが、多くの小さい要素数で最適であり、一般的にも非常に少ない比較回数を実現するアルゴリズムである。
なお、比較回数が常に加減になるとは限らない。
どのような入力が来ても比較回数が一定以下になるという最悪ケースの最小化であり、全入力について平均すると何回の比較になるかを示しているわけではない。
また、ポイントは下記。
1. ペア比較によって、それぞれの小さい要素に上限を作る
2. 対応する大きい要素より前だけを二分探索する
3. 二分探索が最も効率よくなるよう、Jacobsthal順で挿入する